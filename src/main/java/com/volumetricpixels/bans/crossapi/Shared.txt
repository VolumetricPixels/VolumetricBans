The shared package allows for the Per-API system to be used so that some classes can be shared and do not have to 
be dependent on a particular API. This makes it easier to develop new API-Using packages and support new APIs
with VoxelBans!