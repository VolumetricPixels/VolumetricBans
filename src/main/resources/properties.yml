name: ${project.name}
version: ${project.version}
main: com.volumetricpixels.bans.spout.VolumetricBansSpout

author: VolumetricPixels
description: >
            A Global Banning System for SpoutEngine.
            
platform: BOTH
load: STARTUP