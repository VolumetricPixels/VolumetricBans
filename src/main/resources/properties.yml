name: ${project.name}
version: ${project.version}
main: com.volumetricpixels.bans.VolumetricBans

author: VolumetricPixels
authors: [AlbireoX, DziNeIT, kitskub]
description: ${project.description}
            
platform: BOTH
load: STARTUP