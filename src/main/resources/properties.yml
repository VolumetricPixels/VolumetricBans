name: ${project.name}
version: ${project.version}
main: com.volumetricpixels.bans.VolumetricBans

author: VolumetricPixels
authors: [DziNeIT, AlbireoX]
description: ${project.description}
            
platform: BOTH
load: STARTUP